const typeLabels = { duplicate_posting: 'Duplicate posting', unresolved_settlement: 'Unresolved settlement', phantom_charge: 'Phantom charge', ghost_order: 'Ghost order' }
function formatRupees(paise) { return new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR' }).format((paise || 0) / 100) }
function ExceptionCard({ exception }) { const label = typeLabels[exception.exception_type] || exception.exception_type.replaceAll('_', ' '); return <article className="exception-card"><div className="exception-topline"><span className={`exception-tag type-${exception.exception_type}`}>{label}</span><span className="exception-source">{exception.source === 'bank_reconciliation' ? 'Bank' : 'Order DB'}</span></div><div className="exception-main"><div><span className="field-label">Reference</span><strong className="mono">{exception.reference_id}</strong></div><div className="amount"><span className="field-label">Amount</span><strong>{formatRupees(exception.amount_paise)}</strong></div></div><p className="exception-detail">{exception.detail}</p><div className="suggested-action"><span className="action-label"><span aria-hidden="true">↗</span> Suggested action</span><p className={exception.recommended_action ? '' : 'muted'}>{exception.recommended_action || 'No action drafted (LLM unavailable for this run)'}</p></div></article> }
export default ExceptionCard